version: '3.8'

services:
  mongosetup:
    container_name: mongosetup
    image: mongo:4.4
    volumes:
      - ./db/rs-init.sh:/scripts/rs-init.sh
      - ./db/db-setup-entrypoint.sh:/scripts/db-setup-entrypoint.sh
      - ./db/wait-for:/scripts/wait-for
    depends_on:
      - db1
      - db2
      - db3
    restart: "no"
    networks:
      - mongors-network
    entrypoint: [ "./scripts/db-setup-entrypoint.sh" ]

  db1:
    container_name: db1
    image: mongo:4.4
    volumes:
      - ./db/data1:/data/db
    networks:
      - mongors-network
    ports:
      - 27021:27017
    links:
      - db2
      - db3
    restart: always
    entrypoint:
      [
        "/usr/bin/mongod",
        "--bind_ip_all",
        "--replSet",
        "dbrs"
      ]

  db2:
    container_name: db2
    image: mongo:4.4
    volumes:
      - ./db/data2:/data/db
    networks:
      - mongors-network
    ports:
      - 27022:27017
    restart: always
    entrypoint:
      [
        "/usr/bin/mongod",
        "--bind_ip_all",
        "--replSet",
        "dbrs"
      ]

  db3:
    container_name: db3
    image: mongo:4.4
    volumes:
      - ./db/data3:/data/db
    networks:
      - mongors-network
    ports:
      - 27023:27017
    restart: always
    entrypoint:
      [
        "/usr/bin/mongod",
        "--bind_ip_all",
        "--replSet",
        "dbrs"
      ]

  client:
    container_name: client
    build: ./client
    ports:
      - 3000:3000
    networks:
      - mongors-network
    restart: always
    environment:
      - REACT_APP_API_URL=http://192.168.33.26:3001/api

  server:
    container_name: server
    depends_on:
      - db1
      - db2
      - db3
      - client
    build: ./server
    ports:
      - 3001:3001
    networks:
      - mongors-network
    restart: always
    environment:
      - REPLICA_URL=mongodb://db1:27017,db2:27017,db3:27017
      - REPLICA_NAME=dbrs

networks:
  mongors-network:
    driver: bridge
